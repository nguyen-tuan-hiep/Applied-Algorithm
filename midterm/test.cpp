#include<bits/stdc++.h>
using namespace std;

// input an array of integers
// output the number of pairs of integers that have the same remainder when divided by k

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    fflush(stdin);
    freopen("4-input.txt", "r", stdin);
    // input();
    // solve();    
}